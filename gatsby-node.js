// exports.onCreatePage = async ({ page, actions }) => {
//   const { createPage } = actions;
//   if (page.path.match(/^\/(?!dummy).*/)) {
//     page.matchPath = "/:id";
//     createPage(page);
//   } else if (page.path.match(/^\/dummy/)) {
//     page.matchPath = "/dummy/:id";
//     createPage(page);
//   }
// };
